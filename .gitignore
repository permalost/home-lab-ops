# Editors
.vscode/
# Claude Code
.claude/
# k8s
kubeconfig
talosconfig
# Talos — talhelper rendered output (contains decrypted PKI)
clusters/*/clusterconfig/
# certificates and keys
*.pem
*.key
*.crt
# vscode-sops
.decrypted~*.yaml
.config.env
*.agekey
# ESPHome — no SOPS rule for this dir, keep real creds out of git entirely
esphome/secrets.yaml
# Terraform
.terraform
.terraform.tfstate*
terraform.tfstate*
# Brew
Brewfile.lock*
