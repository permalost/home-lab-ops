wifi_ssid: "REPLACE_ME"
wifi_password: "REPLACE_ME"
# Wi-Fi creds are shared across proxies; api_encryption_key/ota_password are
# per-device (each device needs its own) — suffix with _NN matching the
# ble-proxy-NN.yaml file as more are added.
api_encryption_key: "REPLACE_ME"  # esphome generates one on first `esphome run`
ota_password: "REPLACE_ME"
api_encryption_key_02: "REPLACE_ME"
ota_password_02: "REPLACE_ME"
