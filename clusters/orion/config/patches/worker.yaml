# Worker node common configuration
# Applied to all worker nodes

cluster:
  clusterName: orion
  controlPlane:
    endpoint: https://10.50.0.10:6443
  apiServer:
    certSANs:
      - 10.50.0.10
      - orion-api.norseamerican.com
  network:
    dnsDomain: cluster.local
    podSubnets:
      - 10.241.0.0/16
    serviceSubnets:
      - 10.141.0.0/16

machine:
  kubelet:
    nodeIP:
      validSubnets:
        - 10.50.0.0/24
  network:
    # DNS configuration
    nameservers:
      - 1.1.1.1     # Cloudflare fallback
      - 8.8.8.8     # Google fallback
    
    # Extra hosts file entries
    extraHostEntries:
      - ip: 10.50.0.10
        aliases:
          - orion-api.norseamerican.com
          - api.orion.norseamerican.com
