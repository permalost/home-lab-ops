apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: route
spec:
  parentRefs:
    - name: orion
      namespace: gateway
      sectionName: https
  hostnames:
    - ${subdomain}.${domain}
  rules:
    - backendRefs:
        - name: svc
          port: 80
