# Service stays on webapp/base's default port 8080 (matches what
# httproute.yaml's backendRefs.port expects) — but homebox's container
# actually listens on 7745, so targetPort needs to be explicit.
- op: add
  path: /spec/ports/0/targetPort
  value: 7745
