apiVersion: v1
kind: Service
metadata:
  name: vllm-aux
  labels:
    app.kubernetes.io/name: vllm-aux
    model: qwen3.6-35b-a3b
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vllm-aux
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
