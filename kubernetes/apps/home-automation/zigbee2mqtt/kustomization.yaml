apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configMap.yaml
  - deployment.yaml
  - httproute.yaml
  - pvc.yaml
  - service.yaml

labels:
  - includeSelectors: true
    pairs:
      app: zigbee2mqtt
      app.kubernetes.io/name: zigbee2mqtt
