apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configMap.yaml
  - deployment.yaml
  - pvc.yaml
  - service.yaml

labels:
  - includeSelectors: true
    pairs:
      app: mosquitto
      app.kubernetes.io/name: mosquitto
