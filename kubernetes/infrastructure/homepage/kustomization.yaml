apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homepage
resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml

configMapGenerator:
- name: homepage-helm-chart-values
  files:
  - values.yaml

configurations:
  - kustomizeconfig.yaml
