apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml
  - secret.yaml
  - alertmanager-hermes.sops.yaml

configMapGenerator:
- name: vkms-helm-chart-values
  files:
  - values.yaml

configurations:
  - kustomizeconfig.yaml
