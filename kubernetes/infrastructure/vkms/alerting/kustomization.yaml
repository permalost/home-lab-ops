apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - alertmanagerconfig.yaml
# webhook-hermes moved to infrastructure/vkms/values.yaml
# (alertmanager.config) — the VM operator drops unknown http_config keys
# (e.g. http_headers) from CR-derived receivers.
