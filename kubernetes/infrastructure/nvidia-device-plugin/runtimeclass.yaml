# Referenced by pods as spec.runtimeClassName: nvidia — required for GPU access
# on Talos (nvidia-container-toolkit-lts extension provides the "nvidia" handler).
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
